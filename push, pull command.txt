push : 업로드
pull : 다운로드
* push 전에 항상 pull

<command>
git pull origin master 
: repository에서 해당 폴더로 모든 자료를 한번에 pull

git remote add origin 주소
: repository에서 해당 폴더로 모든 자료를 한번에 pull

git status

git add .

git config --global user.email "~~"
git config --global user.name"~~"

git commit -m "massage"

git push origin master